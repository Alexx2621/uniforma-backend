{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "python -m venv .venv && .venv/bin/python -m pip install -r python-services/facturas-proveedores/requirements.txt && npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "npm run start:railway",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
